{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "lean-spec": {
      "type": "local",
      "command": ["npx", "-y", "lean-spec", "mcp", "--specs-dir", "./specs", "--quiet"],
      "enabled": true
    }
  }
}